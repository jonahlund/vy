[package]
name = "vy-core"
version = "0.3.0"
edition = "2024"
authors = ["Jonah Lund <jonahlund@tuta.io>"]
description = "Write HTML with simple macros"
repository = "https://github.com/JonahLund/vy"
license = "MIT"

[features]
std = []
nightly = []

[dependencies]
itoap = "1"
ryu = "1"

[dev-dependencies]
vy = { path = "../" }
